#if defined(INCLUDED_SOLUTION_HEADER)
static_assert(false, "Won't include multiple solution headers");
#endif

#define INLCUDED_SOLUTION_HEADER

#include <vector>
#include <limits>

namespace p053 {
class Solution {
public:
  int maxSubArray(std::vector<int> &nums) {
  }
};
} // namespace p053
